// Fill in during tutorial! 

let userName: "Sean"
let favoriteFood: string[] = ["pizza", "tacos"]

let user: {userName: string,  age: number, favoriteFood: string} = {
    userName: "sees",
    age:30,
    favoriteFood:"pizza"

}

console.log(user.favoriteFood) //Logs users fav food --> pizza
